package com.myweddingplanner.back.repository;

import com.myweddingplanner.back.model.ImageAllergen;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageAllergenRepository extends JpaRepository<ImageAllergen, Long> {
}
