package com.myweddingplanner.back.repository;

import com.myweddingplanner.back.model.RegaloBoda;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegaloBodaRepository extends JpaRepository<RegaloBoda, Long> {
}
