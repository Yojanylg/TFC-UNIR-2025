package com.myweddingplanner.back.repository;

import com.myweddingplanner.back.model.BodaUsuario;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BodaUsuarioRepository extends JpaRepository<BodaUsuario, Long> {
}
